import { expect } from '@playwright/test';
import * as cc from '../commands';
class accountsPage {
    constructor(page) {
        this.page = page;
    }

    
}

export default accountsPage;
